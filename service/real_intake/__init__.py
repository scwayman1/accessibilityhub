"""Fail-closed foundations for the separate controlled real-document service.

Nothing in this package is imported by the public synthetic reviewer. The
deployed synthetic service therefore cannot acquire a real-document route by
configuration alone.
"""
